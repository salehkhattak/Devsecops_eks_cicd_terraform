apiVersion: apps/v1
kind: Deployment
metadata:
  name: saleh-thoughts-app
  namespace: flask-sql-namespace
  labels:
    app: saleh-thoughts-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: saleh-thoughts-app
  template:
    metadata:
      labels:
        app: saleh-thoughts-app
    spec:
      initContainers:
      - name: wait-for-mysql
        image: busybox:1.35
        command:
        - sh
        - -c
        - |
          echo "Waiting for MySQL to be ready..."
          until nc -z mysql 3306; do
            echo "MySQL is not ready yet — sleeping 3s"
            sleep 3
          done
          echo "MySQL is ready!"
      containers:
      - name: saleh-thoughts-app
        image: salehktk005/saleh-thoughts-app:latest
        imagePullPolicy: Always
        env:
        - name: MYSQL_PORT
          value: "3306"
        - name: MYSQL_HOST
          value: "mysql"
        - name: MYSQL_PASSWORD
          value: "admin"
        - name: MYSQL_USER
          value: "admin"
        - name: MYSQL_DB
          value: "mydb"
        ports:
        - containerPort: 5000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
